{
    "name": "getschedule",
    "description": "Returns the JSON file with the schedule of the specified user.",
    "options": [
        {
            "type": 6,
            "name": "user",
            "description": "User to fetch JSON file from.",
            "required": true
        }
    ]
}