{
    "name": "getschedule",
    "description": "Returns the schedule of the specified user.",
    "options": [
        {
            "type": 6,
            "name": "user",
            "description": "User to fetch schedule file from.",
            "required": true
        }
    ]
}