{
    "name": "getschedulejson",
    "description": "Returns the schedule JSON of the specified user.",
    "options": [
        {
            "type": 6,
            "name": "user",
            "description": "User to fetch schedule JSON file from.",
            "required": true
        }
    ]
}